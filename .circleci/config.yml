version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.6
      - image: circleci/mongo:4.0 
    working_directory: ~/repo
    steps:
      - checkout
      - run: npm install
      - run: npm test